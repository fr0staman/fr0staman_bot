# from core.misc import loop
import asyncio

import aiomysql

# loop = asyncio.get_event_loop()

# async def create_pool(self, *args, **kwargs):
#    return await aiomysql.create_pool(host='localhost',
#                                      db='tbot',
#                                      user='tbot',
#                                      password='TelegramBot123',
#                                      loop=loop,
#                                      autocommit=True)


# db_pool = loop.run_until_complete(create_pool(loop))
